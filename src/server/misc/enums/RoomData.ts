/**
 * Enum for the room status
 */
enum RoomStatus {
  WAITING = "waiting",
  PLAYING = "playing",
  ENDED = "ended",
}

/**
 * Enum for the room data
 */
enum RoomData {
  MAX_PLAYERS = 2,
}

export { RoomStatus, RoomData };
